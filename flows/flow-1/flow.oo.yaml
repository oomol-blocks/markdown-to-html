nodes:
  - node_id: +typescript#1
    title: "TypeScript #1"
    icon: ":skill-icons:typescript:"
    task:
      ui:
        default_width: 450
      inputs_def:
        - handle: markdownText
          description: Input
          json_schema:
            type: string
            ui:widget: text
        - handle: wrapWithHtml
          json_schema:
            type: boolean
        - handle: defaultStyle
          json_schema:
            type: boolean
      outputs_def:
        - handle: htmlText
          description: Output
          json_schema:
            type: string
      executor:
        name: nodejs
        options:
          entry: scriptlets/+scriptlet#1.ts
    inputs_from:
      - handle: wrapWithHtml
        value: true
      - handle: defaultStyle
        value: false
      - handle: markdownText
        value: "1. ARIA标签 2. 键盘导航 3. 屏幕阅读器 4. 颜色对比度 5. HTML语义化支持 "
